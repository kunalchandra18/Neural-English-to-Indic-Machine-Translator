# Dependencies for the Gradio front-end (app.py). The hosted deployment uses the
# Streamlit front-end and requirements.txt instead.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.9,<3
numpy
PyYAML
huggingface_hub>=0.24,<1.0
# Needs Python 3.10+; the model wheel must track spaCy's minor version.
spacy>=3.8.10,<3.9
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# gradio 4.x imports HfFolder, which huggingface_hub removed in 1.0.
gradio>=4.44,<5
